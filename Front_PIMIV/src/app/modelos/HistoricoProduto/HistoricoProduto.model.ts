export interface HistoricoProduto {

    // Propriedades
    id: number;
    produtoId: number;
    precoProduto: number;
    dataAlteracao: string;

    // Relacionamentos
    // produto?: Produto;
}