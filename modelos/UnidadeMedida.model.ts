export interface UnidadeMedida {

  // Propriedades
  id: number;
  nome: string;
  abreviacao: string;

  // Relacionamentos
  // produto?: Produto[]; // Inclua se precisar dos produtos relacionados
}