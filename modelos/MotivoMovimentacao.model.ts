export interface MotivoMovimentacao {

    // Propriedades
    id: number;
    tipoMovimentacao: string;
    ativo: boolean;

    // Relacionamentos
    // entrada?: Entrada[];
    // saida?: Saida[];
}